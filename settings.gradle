rootProject.name = 'main-router'

// End-to-end driver/validator subproject: drives requests -> routers -> results and validates.
include 'e2e-driver'

// Express-path consumer: consumes social-express, calls the social API, produces DownstreamResult.
include 'social-express-router'
